"""Rendering: figures and tables. Matplotlib only, no other plotting library.

Every figure whose data derives from realised weather must say so in its
caption or title (`CLAUDE.md` §2.1). The helpers here label the oracle series
explicitly rather than leaving it to the caller to remember.
"""
